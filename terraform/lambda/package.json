{
  "name": "magic-request-lambda",
  "version": "1.0.0",
  "description": "Lambda function for magic candle requests",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@google/genai": "^0.3.0",
    "aws-sdk": "^2.1528.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}